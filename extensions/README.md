# extensions

Extensions for [pi.dev](https://pi.dev/docs/latest/extension://pi.dev/docs/latest/extensions)
use `pi -n "test session"  -nbt -ne -ns -nc --no-themes -np --model <provider/model> --thinking off "ping" -e <extension.ts>` to test an extension without clutter



